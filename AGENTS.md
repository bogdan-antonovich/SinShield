# Repository working agreement

- Never run `git commit` or `git push`.
- Never create or push Git tags, and never publish GitHub releases.
- The user exclusively performs all commits, pushes, tags, and releases.
- Restrict Git usage to read-only inspection, such as `git status`, `git diff`, `git log`, and `git show`.
- Never add eyebrow or kicker labels to the website UI.
