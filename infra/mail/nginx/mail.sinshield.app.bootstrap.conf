server {
    listen 80;
    listen [::]:80;

    server_name mail.sinshield.app;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 503;
    }
}
