# This file must load before sinshield-releases-auth.conf because Nginx builds each
# map as it parses it.
# Bearer tokens may be up to 128 characters. Reserve enough room for the longest
# configured key plus Nginx's hash-element metadata on 64-bit hosts.
map_hash_bucket_size 256;
